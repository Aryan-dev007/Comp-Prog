#include<bits/stdc++.h>
using namespace std;

int main(void)
{
    int n;
    cin >> n;
    bool flag = false;
    while(n--) {
        int k;
        cin >> k;
        if (k == 1) {
            flag = true;
        }
    }
    return 0;
}
