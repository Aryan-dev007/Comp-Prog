#include <bits/stdc++.h>
#define pb push_back
#define lli long long int
#define endl '\n'

using namespace std;

void solve()
{
    lli  a, b, c, d;
    cin >> a >> b >> c;
    cout << a+b+c-1 << endl;
}

int main()
{
    std::ios_base::sync_with_stdio(false);
    int t;
    cin >> t;

    for (int i = 0; i < t; ++i)
    {
        solve();
    }

    return 0;
}

